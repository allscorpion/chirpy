# chirpy

A project inspired by twitter built for learning about HTTP servers in Go. It has built in auth using access/refresh tokens. Multiple endpoints for CRUD operations following a REST architechture.

## ⚙️ Installation

Inside a Go module:

```bash
go get github.com/allscorpion/chirpy
```
